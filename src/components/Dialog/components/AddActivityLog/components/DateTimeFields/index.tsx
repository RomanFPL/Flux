import React from "react";
import DateTimeFieldsRoot from "./DateTimeFields";

const DateTimeFields = (props: React.ComponentProps<typeof DateTimeFieldsRoot>) => {
    return <DateTimeFieldsRoot {...props} />;
};

export default DateTimeFields;
