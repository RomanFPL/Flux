import { Stack, Typography, styled } from "@mui/material";

export const StyledTypography = styled(Typography)({
    fontWeight: "bold",
    fontSize: "37px"
});

export const StyledExpandButtons = styled(Stack)({
    alignItems: "center"
});
